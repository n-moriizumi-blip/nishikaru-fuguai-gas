---
aliases:
  - "品質不具合管理システム CLAUDE"
---

# 引継資料：品質不具合管理システム(新規構築プロジェクト)

## 1. これは何か
現行の「検査不具合報告」システム(`検査不具合報告\コード.js`)を全面的に作り直すプロジェクト。旧フォルダ名は「検査不具合報告_Ver.2.0」だったが、バージョンアップではなく作り直しのため「品質不具合管理システム」に改名(2026-08-02)。

**大原則:** 既存の検査不具合報告(GAS＋Googleフォーム)・差し戻し品一覧表(手入力)は一切変更しない。新システムを別物として並行構築し、安定後に統合・データ移行を検討する。

## 2. 全体スコープとフェーズ
1. **Phase 1(現在着手中)**: KP(社内不良)＋差し戻し品の「クイック入力」システム
2. **Phase 2(後回し)**: CC(客先クレーム)の連絡票チェックリスト管理(生管・品証・加工者・社長が各自の担当項目を更新していく進行中案件の管理)
3. **Phase 3(スプレッドシートのみ2026-08-11着手、アプリ化は未定)**: 不適合改善計画書(なぜなぜ分析・対策・効果確認・水平展開)の管理。CC・KP(品証)・SK(製造工程)の3台帳を追加済み(下記4章「⑤⑥⑦」参照)。入力はまだスプレッドシート直接入力のみで、Webアプリ化するかは未合意。
4. **Phase 4(後回し)**: 別プロジェクト「加工ナレッジ」との連携・閲覧。詳細は`加工ナレッジ\CLAUDE.md`参照(要約は本ファイル末尾の「6. 加工ナレッジプロジェクトとの関係」)
5. **Phase 5(最後)**: 既存システムとの統合・データ移行
6. **Phase 6(後回し・改善計画書台帳のみ2026-08-11着手)**: 「製造工程内不良」の報告。検査で見つかる不良(Phase 1のKP・差し戻し)とは別に、**加工者自身がその場で申告する**工程内不良を記録する仕組み。ユーザーいわく「今は関係ないが、後ほど作成して統合したい」とのことで、日々の記録・入力アプリの詳細設計はまだ行っていない。Phase 1で「処置区分」列(社内不良(KP)／差し戻し)を導入した設計思想と相性が良く、将来的には同じ「不良〇月」シートに処置区分の選択肢を追加する形(例:「工程内不良(自己申告)」)で統合できる可能性がある。なお改善計画書の進捗管理用シート(社内不良管理台帳(製造工程)(SK))はPhase 3の一環として2026-08-11に追加済み(下記4章「⑤⑥⑦」参照)。日々の不良記録アプリ自体の着手時は改めてユーザーに詳細をヒアリングすること。

## 3. アーキテクチャ方針(確定、2026-08-02にAndroidアプリ案から変更)
- **入力の入り口はAndroidネイティブアプリではなく、Webアプリ(GitHub Pages + GAS API)**。[[project-kintai-nishikaru-handover]]の「勤怠申請アプリ」(`勤怠管理システム\shuusei-app\`、GitHub Pages上の`n-moriizumi-blip/nishikaru-shuusei-app`、バックエンドは既存GAS Web Appに`mode`/`action`分岐を追加)と全く同じ構成を踏襲する。
  - フロントは`index.html`1枚(HTML/CSS/JS)、GitHub Pagesでホスティング(PC・タブレットどちらのブラウザからもアクセス可能)。
  - 本人確認はGoogle Identity Services(クライアント側でIDトークン取得→GAS側で`tokeninfo`照会)。
  - バックエンドは新規GASプロジェクト(このスプレッドシートに紐づく)の`doGet`/`doPost`に`mode`/`action`分岐を持たせる、勤怠申請アプリと同じ設計。
  - QRコードのスキャンはブラウザのカメラAPI(`getUserMedia`)＋JS製QRデコードライブラリ(例: jsQR)で実現する想定(ネイティブアプリ不要)。
- **QRコード活用**: 作業票に既に印字されている製造番号のQRコードをスキャンし、「進捗状況照会」シート(ID: `1F9Iu5t62WDW5lg_eeEa6XW9ngCUJ2DmXTKjqd5oXrac`、I-Pro連携データ)を製造番号で検索して得意先名・品番(図番)を自動入力する。
- 実装前にWebモックアップ(Artifact)で画面イメージを確認してから、実際のWebアプリ(GitHub Pages)を実装する。
- **デザイン案「A. 勤怠アプリ踏襲」で確定(2026-08-03)**: 3案(A.勤怠アプリ踏襲/B.検査タグ/C.デジタルノギス)をArtifactモックアップで比較し、Aに決定。勤怠申請アプリ(`shuusei-app`)と同じ配色を踏襲する: ヘッダー濃紺`#1F3A52`＋山吹色アクセント`#F2A900`、背景`#E9ECEF`、カード白背景。フォントも同じ日本語システムフォント(`"Hiragino Sans","Yu Gothic UI","Meiryo",sans-serif`)。
- **見出し名はI-Pro(進捗状況照会)の表記に合わせて統一する**(例: 「客先」「得意先」は「得意先名」に、「図番」は「品番(図番)」に統一)。QR自動入力時にI-Proのデータをそのまま流し込みやすくするため。
- **長期ビジョン(ユーザー方針)**: ゆくゆくは加工ナレッジ等も含めて全部を1つの統合アプリにしたい。今回Web(GitHub Pages)構成に揃えたのはこの布石でもある。
- **アプリ名は「西軽精機アプリ」で確定(2026-08-10、仮名のまま採用)**。リポジトリ名(`nishikaru-kensa-app`、2026-08-11に`nishikaru-app`から改名)とは別に、画面表示・`manifest.json`の`name`として使っていたが、下記の通り2026-08-11のうちにルート統合アプリへ組み込まれ「品質保証課アプリ」に改称された。

## 3.5 実装状況(2026-08-11時点)
- **GitHubリポジトリ**: [`n-moriizumi-blip/nishikaru-kensa-app`](https://github.com/n-moriizumi-blip/nishikaru-kensa-app)(public、`shuusei-app`と同じ構成。2026-08-11にリポジトリ名を`nishikaru-app`から`nishikaru-kensa-app`へ変更)。
- **【重要・2026-08-11】このリポジトリが[[project-ipro-shinchoku-nishikaru-handover]]の「進捗状況照会」QRスキャンアプリ群と統合され、ルートに共通のアプリ選択画面(タイトル「品質保証課アプリ」)ができた**(作業自体は`進捗状況照会`側のセッションで実施)。ルート選択画面から「品質不具合報告」(＝本プロジェクトのアプリ)・「検査担当割付」・「検査開始前保留」の3つを選べる(洗浄工程は対象外、独立のまま)。
  - **本プロジェクトのアプリは`defect-report/`フォルダへ移動**。公開URLが変わっている: 旧 `https://n-moriizumi-blip.github.io/nishikaru-kensa-app/` → 新 **`https://n-moriizumi-blip.github.io/nishikaru-kensa-app/defect-report/`**
  - `manifest.json`・`favicon.svg`・アイコン画像はリポジトリルート共通のものを使う形に変更され、`index.html`側の参照も`../manifest.json`等の相対パスに調整済み(2026-08-11にClaude自身がGitHub上のファイルを確認し、パスが壊れていないことを確認済み)。
  - 「西軽精機アプリ」という名称は使われなくなり、統合後は「品質保証課アプリ」に一本化されている。
- **[WebApi.gs](WebApi.gs)はGASエディタへ貼り付け・デプロイ済み**。デプロイURLを`index.html`の`GAS_URL`定数に設定し、実データでのQR検索・スプレッドシート書き込みの両方を動作確認済み(2026-08-11)。
  - `doGet(mode=lookup)`: 「進捗状況照会」シートを製造番号で検索し、得意先名・品番(図番)を返す。
  - `doGet(mode=masters)`: **「組織図マスタ」スプレッドシート**(ID: `1fffjE_bwrzswvRO62U0OHwvqrs5b_UuSV5IbudUMxec`)の「プルダウン用」シートから、機種名(機械名列)・加工者名(加工者名列)の一覧を返す。フォームの機種名・加工者はこれを使ったプルダウン選択式(2026-08-11、自由入力から変更)。
  - `doPost(action=submit)`: 「不良〇月」シートの次の空き行(A列・L列で判定)へ1件書き込む。不良項目が複数ある場合はL・M列だけの追加行を足す。**J列(良品数)は数式のため書き込み対象から除外**(下記4章参照)。
- **QRの中身はI-Pro専用URL**(例: `http://192.168.0.131:80//workresult/index/FA01/H260400284`)であり、製造番号そのものではないことが実機テストで判明(2026-08-11)。`index.html`の`extractMfgNo()`がURL末尾の区間だけを取り出してから検索するよう対応済み。
- **入力項目の改善(2026-08-11、実機フィードバックにより)**:
  - 「数量」→「加工数」に改名(何の数量か分かりにくいとの指摘)。
  - 「良品数」の手入力欄を廃止。スプレッドシート側でJ列を「加工数−不良数計」の自動計算式にした。
  - 機種名・加工者を自由入力からプルダウン選択に変更(「組織図マスタ」の「プルダウン用」シートを参照)。
  - 入力欄・プルダウンのプレースホルダー文字色をグレー(`#8A93A0`)に統一。プルダウンは`required`+CSSの`:invalid`擬似クラスで「未選択」状態をグレー表示している。
- **スプレッドシートの見やすさをさらに改善(2026-08-11)**: 背景色の色分けに加えて、①処置区分の行色分けに合わせた文字色(社内不良(KP)=紺文字、差し戻し=茶文字)、②表全体の薄いグリッド罫線、③ヘッダー行下の太い罫線、④不良集計・不良集計(キズ原因)シートでの分類/原因コードが変わる行の区切り線、⑤月次サマリーのKP/差し戻し/合計ブロックの区切り線、を追加。あわせて不良集計・不良集計(キズ原因)・月次サマリーの3シートで「〇月」列の幅を50に、列固定位置も「〇月列の手前」(不良集計=B列まで、不良集計(キズ原因)=C列まで、月次サマリー=A列までで変更なし)に統一。
- **Google Identity Servicesでのログインを実装済み(2026-08-11)**。
  - **OAuthクライアントID**: 「品質保証課アプリ」という名前でGoogle Cloud Consoleに新規発行(勤怠申請アプリとは別のクライアントIDを使う方針。理由: 共用すると同意画面に「勤怠申請アプリ」という名前が出て紛らわしいため)。承認済みJavaScript生成元は`https://n-moriizumi-blip.github.io`。クライアントIDは`index.html`の`GOOGLE_CLIENT_ID`と`WebApi.gs`の`OAUTH_CLIENT_ID`の両方に設定済み。
  - **画面フロー**: アプリ起動時にまずログイン画面(Googleサインインボタン)を表示し、ログイン成功後にQRスキャン画面へ進む(ログイン必須)。
  - **本人確認の方式**: クライアント側はIDトークン(`idToken`)を取得して送信時に一緒にPOSTするだけで、実際の検証はGAS側の`verifyGoogleToken_`がGoogleの`tokeninfo`エンドポイントに照会して行う(audienceの一致・メール確認済みフラグ・`nishikaru.co.jp`ドメイン制限をチェック)。クライアントが送ってきたメールアドレスは信用せず、検証済みの値を品証担当者(C列)に記録する。勤怠申請アプリと同じ設計方針。
  - QR検索(`mode=lookup`)・プルダウン取得(`mode=masters`)はログイン不要のまま(読み取り専用のため)。ログインが必要なのは送信(`action=submit`)のみ。
  - **ログインは自動化(2026-08-11)**: `auto_select:true`＋`google.accounts.id.prompt()`で、ブラウザに既にログイン中のGoogleアカウントがあれば自動的にサインインする(One Tap)。自動ログインできない場合(初回・複数アカウント・シークレットモード等)に備え、手動のサインインボタンも並行して表示している。
  - **【実機で発覚・対応済み】初回デプロイ時に送信が「Failed to fetch」で失敗した**: `verifyGoogleToken_`がこのプロジェクトで初めて`UrlFetchApp`(Google以外の外部サイト`oauth2.googleapis.com`へのアクセス)を使ったため、GAS側の権限再承認が必要だった。`WebApi.gs`に1回だけ手動実行する`testExternalFetchAuth()`関数を追加し、これを実行して承認ダイアログを通すことで解消する設計にした(手順は`WebApi.gs`冒頭のコメント参照)。
  - **【教訓・[[feedback-gas-underscore-function-dropdown]]参照】** 当初この関数を`testExternalFetchAuth_`(末尾アンダースコア)としていたが、GASの「実行」プルダウンは末尾が`_`の関数を表示しない仕様のため、ユーザーが手動実行できなかった。`testExternalFetchAuth`(アンダースコアなし)に改名して解消。今後、人が手動実行する想定の関数には末尾アンダースコアを付けないこと。
  - **品証担当者はメールアドレスではなく氏名を表示(2026-08-11)**: `verifyGoogleToken_`の戻り値を`{email, name}`に変更し(Googleのtokeninfoレスポンスに`name`が含まれる)、C列には`name`(取得できなければ`email`)を書き込むようにした。他の列(加工者・担当者2等)も氏名表記のため統一感が出る。
- 専用アプリアイコン(PWA用192px/512px)は未作成。仮のfavicon.svg(紺地に山吹色の六角形)のみ。`勤怠管理システム\資料\西軽精機ロゴ.jpg`(会社ロゴ)の流用か新規デザインかは未決定。
- **フォーム項目の追加(2026-08-11、実機フィードバック第2弾)**:
  - **単価の入力欄を追加**(社内不良(KP)を選んだときだけ表示、差し戻しでは非表示)。これまで単価の入力経路が無く、金額(自動計算)が常に空欄になっていた。
  - **不良項目ごとの「詳細」自由記述欄を追加**(例: 実測値「φ10.05」など)。不良項目を複数追加した場合はそれぞれに詳細を持たせられる。スプレッドシート側に新設した「不良項目詳細」列(N列、下記4章参照)に対応。
  - **キズ原因(任意)のプルダウンを追加**。圧痕・打痕など、キズ系の不良の原因分析に使う欄(現行「キズ集計管理台帳」のA〜H・18項目)。これまで入力経路が無く、「不良集計(キズ原因)」シートが常に0件のままだった。
  - **不良項目が複数ある場合、追加行にも処置区分(B列)の値を複製するよう変更**。これにより、複数不良項目の2行目以降も1行目と同じ色(社内不良(KP)=薄青／差し戻し=薄橙)で表示されるようになった(以前は追加行が無色のままで不自然だった)。件数の集計はB列だけでなくA列(タイムスタンプ、メイン行のみ)も条件に加えて、追加行を二重カウントしないようにしている(下記4章参照)。
- **細部の見た目・入力の調整(2026-08-11、実機フィードバック第3弾)**:
  - 良品数の見出しから「(自動計算)」を削除(冗長との指摘、金額・不良率の見出しにも元々付けていなかったため統一)。
  - **U列(キズ原因)・V列(送信ID)を薄いグレー文字による「見えにくくする」方式から、`hideColumns()`による実際の列非表示に変更**。行の色分け(背景・文字色)の対象外にしていたため、色付きの行でこの2列だけ見た目が浮いて見えていた不具合を解消。
  - **担当者2(O列)をアプリのフォームに追加**。「不良項目ごとに原因を作った担当者が違う場合」だけ使う任意項目のため、既定では非表示にし、「＋ この項目だけ担当者が違う場合は指定する」というリンクを押したときだけ入力欄(組織図マスタの加工者名一覧を流用したプルダウン)が現れるUIにした。不良項目ごとに個別に持てるよう、items配列に`worker2`を追加(`{name, qty, detail, worker2}`)。WebApi.gsの書き込み範囲もK〜Oの5列(メイン行)・L〜Oの4列(追加行)に拡張した。
  - **各シートの配置を統一**: 全シートで垂直方向を中央揃えに。ヘッダー行、および不良集計(不良項目・分類列)・不良集計(キズ原因)(原因コード・分類・原因列)・月次サマリー(項目列)の見出し列は水平方向も中央揃えにした。
- **2026-08-12: QR受付通知(トースト)の文字サイズを拡大**(「進捗状況照会」側のセッションで、他のQRアプリ(洗浄工程・検査担当割付/検査開始前保留)と合わせて実施)。`.toast`のfont-sizeを13.5px→16pxに拡大。**このタイミングで初めて`index.html`のローカルコピーをこのフォルダ直下に作成**(これまでは本プロジェクトのフロントエンドはGitHubリポジトリ側にしか存在せず、Claudeは都度GitHub上のファイルを直接確認していたが、他のQRアプリと同じくローカルにも控えを置く運用に変更)。今後この`index.html`を編集する場合はリポジトリ側(`app/defect-report/index.html`)とローカル側の両方を同期すること。
- **1件ごとの枠線囲み(2026-08-12)**: 旧システム(`検査不具合報告\コード.js`のtransferToMonthlySheet)は送信のたびに、その入力(複数行にまたがる場合はまとめて)を枠線で囲み、次の入力と見分けやすくしていた。新システムにはこの動きが無く「見づらい」と指摘があったため再現した。
  - `WebApi.gs`の`writeDefectRecord_`が、書き込み終わったレコード(不良項目が複数で複数行にまたがる場合も1つの枠)をCOLOR.HEADER_BORDER色・SOLID_MEDIUM太さの外枠だけで囲むようにした(内部に縦線・横線は引かない、2026-08-12にユーザー指摘で縦線を外した)。これで今後のアプリからの送信は自動的に枠が付く。
  - 既に入力済みだった分(6月・7月の移行データ、テスト送信分など)にさかのぼって適用するため、`SetupSpreadsheet.gs`に手動実行用の`addRecordBordersToAllMonths`を追加(全12ヶ月の「不良〇月」シートを走査し、A列・L列の並びからレコードのまとまりを判定して枠線を付ける。何度実行しても結果は同じ)。

## 4. Phase 1 実データ(Drive上に作成済み)
- 格納フォルダ: 「改善計画書台帳」フォルダ配下の**「品質不具合管理システム(改訂中)」**フォルダ(ID: `1cT2gttoIR69MYn2KRV3_E3lv0mjxhVeo`)
- スプレッドシート: **「客先クレーム(CC)・社内不良(KP)管理台帳 2026年度」**(ID: `1xeEybU6fhqvAEM02eEkUwBdOA_mD-fMUqKoCmckUcM0`、2026-08-13に「品質不具合管理システム 2026年度」から改名。年度が変わると別IDのスプレッドシートに自動的に切り替わる、下記「⑩」参照)
- 構築用スクリプト: [SetupSpreadsheet.gs](SetupSpreadsheet.gs)(GASエディタに貼り付けて`setupQualityDefectSystem`を実行するとシート一式が作られる、初期セットアップ専用・再実行するとデータが消える)

### ① 不良〇月シート(A〜W列、ヘッダー1行目・データ2行目〜115行目、2026-08-02にKP・差し戻しを統合)
社内不良(KP)と差し戻しは「1件の不良につきどちらか一方の処置になる」同じ発生イベントのため、1つのシートに統合した(処置区分列で区別)。項目名の重複(「材料名」差し戻し／「材質」KP)は「材質」に統一。「品証担当者」(差し戻し)と「担当者2」(KP)は別役割のため統一していない。

`タイムスタンプ／処置区分(社内不良(KP)／差し戻し)／品証担当者／製造番号／得意先名／品番(図番)／加工者／機種名／設備№／加工数／良品数(自動計算)／不良数計／不良項目(下記40項目から選択)／不良数／不良項目詳細／担当者2(任意)／単価／金額(自動計算)／備考／材質／不良率(自動計算)／キズ原因(列非表示・任意)／送信ID(列非表示)`

- **品証担当者(C列、2026-08-10にM列から先頭グループへ移動)**: 手入力ではなく、**Webアプリでログイン中のGoogleアカウントをGAS側が検証し、氏名を自動的に書き込む**(2026-08-11、当初はメールアドレスだったが氏名表記に変更)。「いつ・何を・誰が入力したか」という記録の身元情報として、タイムスタンプ・処置区分のすぐ後ろにまとめてある。
- **製造番号(D列、2026-08-13新設)**: QRスキャンで既に取得できていた製造番号(`index.html`の`f-mfgno`)を、これまでシートに保存していなかったため追加。得意先名の左に配置。既存の12シートには`SetupSpreadsheet.gs`の`addManufacturingNumberColumn`(1回だけ手動実行、`sheet.insertColumnBefore()`で入力済みデータを保持したまま列を追加)で反映。これによりD列以降(得意先名〜送信ID)が1列ずつ後ろにずれ、`不良集計`等の集計シート側の数式もGoogle Sheetsが自動追従した(作り直し不要)。
- **加工数(J列、2026-08-11に「数量」から改名、2026-08-13の列追加でI列→J列)**: 何の数量か分かりにくいとの指摘で改名。**良品数(K列)は「加工数−不良数計」の自動計算式に変更**(以前は手入力だったが二重入力・不整合のリスクを避けるため)。
- **不良項目詳細(O列、2026-08-11新設)**: 不良項目ごとの自由記述(実測値など)。1件目はメイン行、2件目以降は追加行に、不良項目・不良数とセットで入る。
- 1件の不良につき1行目(メイン行)にA〜J列・L〜P列・Q〜T列を入力(処置区分は必須。K・R・U列は自動計算のため入力不要。単価は社内不良(KP)の場合のみ、差し戻しは空欄でよい。キズ原因(V列)は任意。担当者2(P列)も任意)。同じ不良で不良項目が複数ある場合、2行目以降はB列(処置区分、行の色分け用に複製)・M〜P列(不良項目・不良数・詳細・担当者2)を追加する行を足す。**担当者2はアプリ側で「不良項目ごとに個別入力」の形になっており(2026-08-11)、複数不良項目それぞれで原因を作った担当者が違う場合にだけ、その項目単位で開いて入力する(通常は非表示)。**
- 良品数(K列)＝IFERROR(加工数−不良数計,"")、金額(R列)＝IFERROR(不良数計×単価,"")、不良率(U列)＝IFERROR(不良数計/加工数,"")、いずれも自動計算式。
- **見やすさ向上(2026-08-11)**: ヘッダー行は濃紺背景・白文字、データ行は1行おきの縞模様、処置区分に応じて行全体を色分け(社内不良(KP)=薄青、差し戻し=薄橙)。**複数不良項目の追加行にも処置区分を複製するため、追加行も同じ色になる**(2026-08-11、以前は追加行が無色だった)。

**不良項目マスタ(40項目、2026-08-12に全面差し替え):**
以前は差し戻し品一覧表ベースの29項目だったが、ユーザーから「移行先(不良〇月)のL列プルダウンを、元データ(旧「客先クレーム(CC)・社内不良(KP)管理台帳」の「社内不具合〇月」シート)のG列(不具合内容1)のプルダウンとそのまま同じにしてほしい」と依頼があり、実際のプルダウン(データ!G4:G44、12ヶ月とも同一)をGASで直接調査して丸ごと差し替えた。グループ分け(不良集計の色分け用)は元のプルダウンの並び順から見た区切りを提案し、ユーザー承認済み。
- 寸法系(13): 寸法不良、寸法大、寸法小、外径大、外径小、内径大、内径小、長さ不良、ねじ不良、形状不良、穴ズレ（穴曲り）、テーパ不良、平研不良
- 変形系(3): 曲がり、変形、振れ大
- 材質・混入系(4): 未加工混入（未加工）、材質違い、圧入・接着不良、現品違い
- キズ系(9): キズ、流動キズ、チャックキズ、回収時キズ、落下キズ、材料キズ、加工時キズ、ガイドブッシュキズ、その他キズ
- 外観・その他系(11): バリ、段差、溶着、挽目不良、バフがけ不良、修正不良、取り残し、ムシレ、切粉、汚れ、変色・錆

**マスタ更新の反映方法**: `SetupSpreadsheet.gs`に手動実行用の`updateDefectItemMaster`を追加した。`setupQualityDefectSystem`と違い、既存の「不良〇月」12シートのデータは消さずL列プルダウンの選択肢だけを更新し、実データの無い「不良集計」「改善計画書台帳」3シート(CC/KP/SK)は作り直す。`index.html`(app/defect-report/)のDEFECT_ITEMS配列も40項目に同期済み・push済み。`MigrateOldData.gs`の`mapDefectItemName_`/`KP_KEYWORD_RULES_`(旧自由記述→新マスタへの丸め込みロジック)も新しい語彙に合わせて全面書き換え(新マスタに万能の「その他」項目が無いため、最終フォールバックは「形状不良」に変更)。

### ② 不良集計シート
不良項目(40項目)×月別の自動集計表。処置区分を問わず合算。分類(寸法系/変形系/材質・混入系/キズ系/外観・その他系)ごとに行の背景色を分けて見やすくしてある(2026-08-11)。
**2026-08-12改訂**: 月ごとに「件数(COUNTIF)」「個数(SUMIF)」の2列に分割(それまでは個数のみ)、ヘッダーは2行構成(1行目に月名を2列マージ、2行目に件数/個数)。年計列も件数・個数の2列。最下行に「合計」行を追加し、各列(月ごとの件数・個数、年計の件数・個数)の縦計を表示。共通ロジックは`buildItemSummarySheet_`に切り出し、③のキズ原因版と共有している。

### ③ 不良集計(キズ原因)シート
現行の「キズ集計管理台帳」のA〜H原因コード(19項目)を踏襲した任意項目の集計表(不良〇月シートのU列を参照)。キズ系の不良の原因分析用で、必須入力ではない。原因コードごとに行の背景色を分けてある(2026-08-11)。2026-08-11時点でアプリのフォームにキズ原因の入力欄を追加したため、実際にデータが集まるようになった(それまでは入力経路が無く常に0件だった)。
**2026-08-12改訂**: ②と同様、月ごとに「件数(COUNTIF)」「個数(SUMIFでK列(不良数計)を合算)」の2列に分割、最下行に合計行を追加。個数は今回新設(それまでキズ原因側には件数しか無かった)。

**②③共通・見やすさ追加(2026-08-12)**: 件数/個数を青系の濃淡2段階(`COLOR.SUMMARY_COUNT_BG`薄い方／`COLOR.SUMMARY_QTY_BG`濃い方、当初は色相の違う2色で試したがユーザー指摘で同系色の濃淡に変更)で色分け。当初はヘッダー2行目だけに付けていたが、ユーザーから「見出しだけでなく列全部に」と指摘があり、データ行(合計行を除く)にも同じ濃淡を敷くよう拡張。これに伴い、項目の分類色(寸法系/キズ系など)はデータ行のラベル列(項目名・分類)だけに限定し、件数・個数列とぶつからないようにした(以前は行全体に分類色を敷いていた)。**ユーザーから「A・B列の色をそれぞれ濃淡で」との依頼があり一旦`shadeColor_`/`shadeStepsFor_`で実装したが、直後に「1つ前に戻して」と取り消しの指示があり、ラベル列は分類の色一色(濃淡なし)に戻した。**
**その後、「寸法系の件数は薄い青、個数は濃い青。変形系の件数は薄いオレンジ、個数は濃いオレンジ…」という具体案が出たため(2026-08-12)、件数・個数列も分類ごとの色(グループ色そのまま=件数、少し濃くした色=個数)で塗るよう再修正。GROUP_PALETTEの割り当て順が偶然この指定通り(寸法系→薄青、変形系→薄橙、材質・混入系→薄緑…)だったため、既存のラベル列の色をそのまま件数列にも流用する形で実装できた。ヘッダー2行目(件数/個数の見出し文字)は特定グループに属さないため、従来通り青系の濃淡(`COLOR.SUMMARY_COUNT_BG`/`SUMMARY_QTY_BG`)のまま。
**濃くした色がグレーに寄るバグ修正(2026-08-12)**: 個数列を「黒に混ぜて暗くする」`shadeColor_`で作ったところ、薄いパステルカラーは色相・彩度の差が小さいため明るさだけ下げると色味が抜けてグレーっぽく見えてしまう指摘があった。HSLの明度だけを下げて色相・彩度は保つ`darkenColor_`に置き換えて解消(`shadeColor_`は削除)。個数列の濃さ(`darkenColor_`の第2引数)はユーザーの見た目確認を経て22→12→7ポイントまで薄く調整。
**集計の境目を二重線に(2026-08-12)**: 月ブロックの区切り線は通常SOLID_MEDIUM・薄いグレー(`COLOR.GRID_BORDER`)だが、①最後の月(5月)と年計の境目(縦線)、②最終項目行と最下行「合計」の境目(横線)、の2箇所は`SpreadsheetApp.BorderStyle.DOUBLE`・濃い色(`COLOR.HEADER_BORDER`)にして、どこからが集計(年計・合計)かを分かりやすくした。月ブロックの境目(ラベル列との境目含む)に縦線を追加し、どこからどこまでが同じ月か分かりやすくした。

**手動で調整した列幅を作り直しでも保持(2026-08-12)**: `buildDefectSummarySheet_`/`buildKpCauseSummarySheet_`は`replaceSheet_`でシートを毎回作り直す仕様のため、ユーザーが手動で列幅を調整しても、次にマスタ更新等で作り直すと既定値(45px等)に戻ってしまっていた。`captureColumnWidths_`(新設)で作り直す直前に現在の列幅を記録し、`buildItemSummarySheet_`が既定値を設定した後にその記録(列数が変わっていない場合のみ)で上書きするようにして、手動調整が今後の`updateDefectItemMaster`実行でも引き継がれるようにした。

### ④ 月次サマリーシート
月ごとの「KP不良件数・不良個数・不良金額」「差し戻し件数・個数」(不良〇月シートのB列「処置区分」で按分)、および両方の合計を1枚で表示(現行の「キズ集計管理台帳」内の「月別不良グラフ」相当)。KP行・差し戻し行・合計行をそれぞれ色分けしてある(2026-08-11)。**件数はA列(タイムスタンプ、メイン行のみ記入)が空でないことも条件に加えたCOUNTIFSで集計**(B列は追加行にも複製されるようになったため、単純なCOUNTIFだと二重カウントになる、2026-08-11対応)。

### ⑤⑥⑦ 改善計画書台帳3シート(客先クレーム管理台帳(CC)・社内不良管理台帳(品証)(KP)・社内不良管理台帳(製造工程)(SK)、2026-08-11新設)
既存の「改善計画書台帳」スプレッドシート(CC/KP/SKの3種類が1枚のシートに縦積みで読みにくかった)を参考に、独立した3シートとして追加した。**「不良〇月」(Phase 1、日々の不良件数の記録・自動集計用)とは別物**で、こちらは1件ごとの**不適合改善計画書(なぜなぜ分析→改善策の共有→図面に貼付→加工指示・改善の実施→効果確認→水平展開→水平展開の効果確認)の進捗管理台帳**。台帳番号(CC-0001／KP-00001／SK-0001)は自動採番せず、これまで通り手入力。月別分割はせず、通し番号のまま1枚のシート(150行、超える場合は手動で行を追加)。
- **発生情報部分の列構成は台帳ごとに異なる**: CC=台帳番号・発生日(受領日)・関連文書・客先名・品番(図番)・クレーム内容分類・クレーム内容詳細・NG数(納入数)・加工者・機械名・検査員・出荷区分・単価・金額(自動計算)。KP(品証)=CCとほぼ同じだが出荷区分なし、分類名は「不良内容分類/詳細」に変更(元資料は「クレーム内容分類」を流用していたが意味が合わないため改めた)。SK(製造工程)=客先名・検査員・単価・金額なし(加工者自身が申告する工程内不良のため)。
- **共通のワークフロー列(`WORKFLOW_HEADERS`)は3シートとも同一**: なぜなぜ分析(担当:品証)→改善策の共有(担当:社長)→図面に貼付(担当:社長)→加工指示・改善の実施(担当:加工者)→効果確認(担当:品証)→(効果不十分時の差し戻し文書No.、任意)→水平展開の実施(担当:社長)→水平展開の効果確認(担当:社長)。各ステップに担当欄と完了日欄をセットで持つ(ユーザー要望で元の台帳をフル再現)。
- クレーム内容分類/不良内容分類のプルダウンは①の不良項目マスタ(29項目)を流用。加工者・機械名のプルダウンは組織図マスタ(WebApi.gsの`getMasters_`と同じ取得ロジック)を`SetupSpreadsheet.gs`側でも1回取得して使う。いずれも`setAllowInvalid(true)`(マスタに無い名前でも記録は残せるようにする)。
- **2026-08-11時点でアプリ側の入力経路は未定**。「不良〇月」のようにWebアプリから入力するかどうかはまだ決まっておらず、今のところはスプレッドシートへの直接入力のみ。着手する場合は改めて方針をユーザーと合意すること。
- **【2026-08-12】方針未確定のため3シートとも非表示(`hideSheet()`)にした**。`buildCcLedgerSheet_`/`buildKpQualityLedgerSheet_`/`buildSkProcessLedgerSheet_`の末尾で`sheet.hideSheet()`を呼ぶよう変更(`updateDefectItemMaster`実行時にも非表示が維持される)。
- **【2026-08-15・方針決定】ユーザーが方針を決め、このコード生成版とは別に、旧システム形式(複数行結合ヘッダー)の「本物」のCC/KP/SK台帳3シートを手作業で新規作成し、表示状態で運用を開始した**。このコード生成版(簡易版)は名前末尾に「仮」を付けて区別され、非表示のまま今後も並行して作成され続ける(ユーザー希望)。実質的な運用は「本物」側で行われている。詳細・年度自動ロールオーバーへの対応は下記「⑭」参照。

### ⑧ グラフシート(ダッシュボード、2026-08-12新設)
旧「客先クレーム(CC)・社内不良(KP)管理台帳」の「グラフ」シート(棒・折れ線8枚)を確認したところ、うち4枚が「社内」と「出荷検査」で内容がほぼ重複しており分かりにくかった(新システムには「出荷検査」区分が無く社内不良(KP)／差し戻しのみのため再現不可)。ユーザーと協議の上、以下4種類に整理して`buildDefectDashboardSheet_`(`SetupSpreadsheet.gs`)で新規作成した。
- **①月別不良個数**(棒グラフ、月次サマリー!「合計 不良個数(KP＋差し戻し)」行を参照)
- **②月別不良金額**(折れ線グラフ、月次サマリー!「KP 不良金額」行を参照。差し戻しは単価を追わないため対象外)
- **③分類別内訳**(積み上げ棒グラフ、月別×5グループ。不良集計シートの項目別個数をSUMIFで分類ごとに合算。色は不良集計シートの行色分けと同じ`buildGroupColorMap_`を使い視覚的に対応させている)
- **④キズ原因内訳**(棒グラフ、原因グループ別の年計個数。不良集計(キズ原因)シートをSUMIFで原因グループごとに合算。任意項目のため参考扱い)

シート先頭(A1:O..)に4表の元データ(数式による自動集計、編集不要)を置き、その下にグラフ本体を配置。シートタブの一番左(先頭)に配置される。`setupQualityDefectSystem`・`updateDefectItemMaster`実行時にも自動で作り直される。既存データに影響を与えず単独で追加したい場合は手動実行用の`addDefectDashboardSheet`を使う。

### ⑨ ダッシュボードWebページ(dashboard.html、2026-08-13新設・独立サイト化)
「⑧グラフシート」着手直後、ユーザーから「グラフはやっぱりWebアプリにする」と方針転換の指示があり、スプレッドシート上のグラフ(⑧)ではなくWebページとして実装し直した(⑧のシート・関数はコードとしては残っているが未使用)。
- **手順**: Artifactでモックアップを3パターン(A.シンプル/B.KPI+グラフ/C.詳細分析)提示→ユーザーがCを選択・新規グラフ4種(得意先別・設備別ランキング、不良率推移、KP/差し戻し比率)を承認→さらに「数値常時表示」「グラフ大判・同サイズ統一」「個数・件数を重ね合わせ(棒+折れ線の複合グラフ)」「KPIタイルも統合」の4点修正を反映→「これで進めて」で実装承認、という流れ(モックアップURLはセッション内のみ、恒久リンクは無し)。
- **[WebApi.gs](WebApi.gs)**: `doGet(mode=dashboard)`→`buildDashboardData_()`を追加。「月次サマリー」「不良集計」「不良集計(キズ原因)」の集計値、および「不良〇月」12シートを走査して得意先別金額・設備別個数・月別加工数合計(不良率の分母)を集計し、JSONで返す。`MONTHS`/`DEFECT_ITEMS`/`KP_CAUSE_ITEMS`/`uniqueInOrder_`は同一GASプロジェクトの`SetupSpreadsheet.gs`のものをそのまま参照(グローバル共有、`writeDefectRecord_`が`COLOR`を参照しているのと同じ考え方)。新しい外部サイトアクセスは無いため`testExternalFetchAuth`の再承認は不要、**「新しいデプロイ」での再デプロイのみ必要**。
- **【2026-08-13・重要】表示側は「品質保証課アプリ」(nishikaru-kensa-app)には含めず、完全に独立したGitHubリポジトリ・URLにする方針に変更**(ユーザー指示「品質保証課アプリの中に入れるんじゃなくて、別物にして」)。当初は`app/defect-report/dashboard.html`として品質保証課アプリ内に追加し、`index.html`に導線ボタンも設置していたが、これらは削除・巻き戻し済み(nishikaru-kensa-appコミット`2c31f54`)。
  - **新リポジトリ**: [`n-moriizumi-blip/nishikaru-fuguai-dashboard`](https://github.com/n-moriizumi-blip/nishikaru-fuguai-dashboard)(public、GitHub Pages有効、mainブランチ直下を公開)
  - **公開URL**: `https://n-moriizumi-blip.github.io/nishikaru-fuguai-dashboard/`
  - リポジトリ直下に`index.html`(ダッシュボード本体)・`manifest.json`・`favicon.svg`・`icon-192.png`・`icon-512.png`(favicon/iconは品質保証課アプリと共通デザインを流用)。品質保証課アプリへの導線・被リンクは無し(完全に独立、ブックマーク等で直接開く運用)。
  - ローカルの`dashboard.html`(このフォルダ直下)は、この**新リポジトリ**の`index.html`のローカル控え(品質保証課アプリの`index.html`とは別物なので注意)。編集時は新リポジトリ側にも反映すること。
- **中身**(変更なし): ログイン不要(`mode=dashboard`は未ログインでも呼べる設計)。月別不良個数・件数(棒+折れ線の複合グラフ)、月別不良金額(KPのみ)、分類別内訳(積み上げ棒)、キズ原因内訳、不良率推移(折れ線)、KP/差し戻し比率、得意先別・設備別ランキングの8枚のグラフ+KPIタイル4枚(今月不良個数・金額・不良率・KP比率、前月比・スパークライン付き)。SVGを自前描画(外部チャートライブラリ不使用)、数値は常時ラベル表示+ホバー/フォーカスでツールチップ、各グラフに「表で見る」トグルで数値表示切替可能。GAS_URLは`index.html`(品質保証課アプリ側)と同じデプロイURLを使う(オリジンが同じ`n-moriizumi-blip.github.io`のため別リポジトリでもCORS等の問題なし)。
- **ユーザー側の残作業**: `WebApi.gs`の最新版をGASエディタに貼り替えて「新しいデプロイ」で再デプロイすること(これをしないと`mode=dashboard`が404/unknown modeのままになる)。
- **【2026-08-13・デザイン確定】** Artifactで3スキン(A.スタンダード/B.月報スタイル/C.モニターボード)のデザイン案を提示し、ユーザーが**A.スタンダード**(現行の濃紺+山吹色、白カード)を選択。あわせて「1グラフ1列」「PC/タブレット/スマホで表示サイズを自動調整」の指示があり反映済み。
  - `.grid`(複数列)を`.stack`(縦1列)に変更。
  - `.wrap`の最大幅をブレークポイントで可変に(デスクトップ1180px／タブレット780px(980px以下)／スマホ全幅(600px以下))。
  - グラフのSVG `viewBox`もブレークポイントごとに`GEOMETRY`(`W`/`H`/`M`/`barMax`/`labelEvery`)を用意し、`window.innerWidth`から選択(`breakpointName()`)。viewBoxの単位を実際のCSS px幅にほぼ揃えることで、画面サイズが変わってもグラフ内の文字サイズが極端に縮小・拡大しないようにしている。
  - スマホ幅(`labelEvery:2`)では月別グラフの横軸ラベル(月名)を1つ飛ばしで間引き、値ラベル(常時表示の数値)は間引かない。
  - `resize`イベントに150msデバウンスを付け、ブレークポイントが実際にまたいだ時だけ再描画(データの再取得はしない)。
- **【未対応】「西軽精機 アプリ目次」スプレッドシートへの追記が必要**(ルートCLAUDE.mdの運用ルールにより、Claudeはセル直接編集不可)。追加してほしい行: アプリ名「品質不良ダッシュボード」／URL `https://n-moriizumi-blip.github.io/nishikaru-fuguai-dashboard/`／備考「品質保証課アプリとは独立、GASバックエンドは品質不具合管理システムと共通」。

### ⑩ ファイル名変更・年度自動ロールオーバー(2026-08-13新設)
以前保留にしていた2件(旧システムと同じ名前への変更、新年度スプレッドシートの自動作成)にユーザーから着手の指示があり、あわせて実装した。着手前に確認した前提: **旧システム(Googleフォーム＋旧コード.js)は完全に退役済み(新システムに完全移行済み)**であり、ファイル名の衝突が旧システムの動作に影響することは無い。
- **ファイル名変更**: 新システムのスプレッドシートを「品質不具合管理システム 2026年度」から**旧システムと同じ命名規則**「客先クレーム(CC)・社内不良(KP)管理台帳 2026年度」に変更。`SetupSpreadsheet.gs`の`renameToLegacyNaming`(1回だけ手動実行)で実施。**IDは変わらないため他のコードへの影響は無い。**
  - **【トラブル対応・2026-08-13】** 当初案は、名前が重複する旧システムの2026年度ファイル(退役済み、`MigrateOldData.gs`の`MIGRATE_KP_SOURCE_SS_ID`、ID `1JRnbkwY5VF93dLTpyl2yRvaz5XOom6vM4_C6x54o5cM`)の末尾にも「(旧アーカイブ)」を付けて区別する設計だったが、`renameToLegacyNaming`実行時に`Exception: Access denied: DriveApp.`で失敗。まず「OAuthスコープの再承認不足」を疑い`appsscript.json`確認(`oauthScopes`手動指定なし)・Googleアカウント側でのアクセス権削除→再承認を試したが解消せず。実行ログの失敗行を精査した結果、**新システム側の名前変更(自分自身のファイル)は例外の前に成功しており、失敗していたのは旧システムの退役済みファイルの名前変更だけ**と判明。旧ファイルは共有ドライブ内にあり、このGoogleアカウントには編集権限が無いことが根本原因(スコープの問題ではなく個別ファイルの権限の問題)。
  - **対応**: 旧ファイルを触る必要が無いよう設計を変更。`getOrCreateYearSpreadsheet_`の検索範囲を`DriveApp.getFilesByName`(Drive全体)から`SPREADSHEET_PARENT_FOLDER_ID`フォルダ直下だけの検索(`folder.getFilesByName`)に変更した。旧ファイルは「品質不具合管理システム(改訂中)」フォルダの外(「改善計画書台帳」フォルダ直下)にあるため、名前が重複していてもこの検索には引っかからず、旧ファイルの権限問題を回避しつつ実質的な影響も無い。`renameToLegacyNaming`は新システム側の名前変更のみを行う形に簡素化した。
- **年度自動ロールオーバー**: 旧コード.jsの`getOrCreateYearSpreadsheet`と同じ考え方を新システムにも導入。`SetupSpreadsheet.gs`に以下を追加:
  - `SPREADSHEET_NAME_PREFIX`(`'客先クレーム(CC)・社内不良(KP)管理台帳 '`、末尾スペース含む)・`SPREADSHEET_PARENT_FOLDER_ID`(格納先フォルダ、上記④参照)
  - `getCurrentFiscalYear_(date)`: 6月始まり年度の判定(月≧6ならその年、そうでなければ前年)
  - `getOrCreateYearSpreadsheet_(fiscalYear)`: 該当年度のファイルをDriveで名前検索、無ければ`SpreadsheetApp.create`で新規作成→`SPREADSHEET_PARENT_FOLDER_ID`のフォルダへ移動→`setupQualityDefectSystemFor_`で不良12シート・集計3シート・グラフ・改善計画書台帳3シートを組み立て→既定の「シート1」を削除
  - `getCurrentYearSpreadsheet_(date)`: 上記2つを組み合わせ、「今日時点の年度のスプレッドシート」を返す(無ければ自動作成)
  - `setupQualityDefectSystem()`は`setupQualityDefectSystemFor_(ss)`を呼ぶ薄いラッパーに分割(対象スプレッドシートを引数化し、年度自動作成からも使い回せるようにした)。**`setupQualityDefectSystem()`自体は初期セットアップ専用のため、あえて`SPREADSHEET_ID`固定のままにしてある**(誤って別年度のデータを一括作り直ししてしまう事故を避けるため)。
  - `updateDefectItemMaster`/`syncMonthlySheetLayout`/`addRecordBordersToAllMonths`/`addDefectDashboardSheet`(手動実行系4関数)と、`WebApi.gs`の`writeDefectRecord_`/`buildDashboardData_`は、すべて`SpreadsheetApp.openById(SPREADSHEET_ID)`固定から`getCurrentYearSpreadsheet_()`呼び出しに変更。これにより6月になって新年度に入ると、次のアクセス時に自動的に新しいスプレッドシート(無ければその場で自動作成)へ切り替わる。**Webアプリのデプロイ・GAS_URL・OAuthクライアント等はそのまま(GASプロジェクト自体は特定のスプレッドシートに紐付いているが、コードは`openById`で明示的に対象を開くため、どの年度のデータを操作するかはプロジェクトの紐付け先と無関係)。**
  - `WebApi.gs`の`TARGET_SS_ID`定数は不要になったため削除。
- **ユーザー側の残作業**: ①`SetupSpreadsheet.gs`の最新版(修正版)をGASエディタに貼り替え→`renameToLegacyNaming`を再実行(新システム側の名前変更のみ行う版、既に1回成功しているはずだが再実行しても無害)。②`WebApi.gs`をGASエディタに貼り替えて「新しいデプロイ」で再デプロイ(新しい外部アクセスは無いため`testExternalFetchAuth`の再承認は不要)。
- 「西軽精機 アプリ目次」等の外部ドキュメントにこのスプレッドシートの名前を書いている箇所があれば、新しい名前への更新が必要(Claudeはセル直接編集不可)。
- **【トラブル対応その2・2026-08-13】`SPREADSHEET_PARENT_FOLDER_ID`のフォルダ違いにより、アクセスのたびに重複スプレッドシートが自動作成される事故が発生**。実際の2026年度ファイル(1xeEybU6...)は「改善計画書台帳」フォルダ直下にあるが、`SPREADSHEET_PARENT_FOLDER_ID`は誤って1つ下の「品質不具合管理システム(改訂中)」フォルダ(`1cT2gttoIR69MYn2KRV3_E3lv0mjxhVeo`)を指していたため、`getOrCreateYearSpreadsheet_`の検索が常に「見つからない」と判定し、アクセスのたびに`setupQualityDefectSystemFor_`(不良12シート等の全組み立て、数分かかる重い処理)を伴う新規スプレッドシートを自動作成し続けていた。これが「ダッシュボードが読み込み中のまま」「不具合入力しても(本来のファイルには)書き込まれない」の原因。ユーザーが自動生成された重複ファイルをその都度手動削除して発覚・対応。**`SPREADSHEET_PARENT_FOLDER_ID`を実際の格納先「改善計画書台帳」フォルダ(`1zWL_FZ_yMjx5pTDyK6fdQf0Tdkf-wvrC`)に修正した**(新規作成先も同じフォルダになる。他の古いファイル・テンプレート等と同じ階層に置かれる点は許容)。
- **UX改善**: `index.html`の送信ボタンに送信中インジケータを追加(押した後「送信中…」表示＋ボタン無効化、完了/失敗で元に戻す)。処理に時間がかかる場合(上記バグの影響含む)でも反応が分かるように。

### ⑪ 製造番号列の追加(2026-08-13)
ユーザーから「不良〇月シートに製造番号も記載する列を、得意先名の左に追加してほしい」と依頼。QRスキャンで取得した製造番号は`index.html`の送信データ(`mfgNo`)に**既に含まれていた**が、`WebApi.gs`側で受け取るだけでシートに保存していなかったため、書き込み処理を追加する形で対応。
- **列構成が1つ後ろにずれる**: D列に「製造番号」を新設したため、旧D列(得意先名)以降(得意先名〜送信ID)がすべて1列ずつ後ろにずれた(新しい列構成は上記4章「①」参照)。影響範囲: `SetupSpreadsheet.gs`(`buildDefectMonthlySheet_`のヘッダー・列幅・数式・プルダウン列・非表示列、`buildDefectSummarySheet_`/`buildKpCauseSummarySheet_`の集計数式が参照する列文字、`buildMonthlySummarySheet_`の集計数式、`updateDefectItemMaster`・`addRecordBordersToAllMonths`の列番号)、`WebApi.gs`(`writeDefectRecord_`の書き込み列、`findNextRow_`の判定列、`buildDashboardData_`の得意先名/機種名/加工数/不良数計/金額の読み取り列)。
- **既存データを保持したまま反映**: `replaceSheet_`で作り直すと入力済みデータが消えてしまうため、新規関数`addManufacturingNumberColumn`(`SetupSpreadsheet.gs`、1回だけ手動実行)を追加。`sheet.insertColumnBefore(4)`でD列に空列を挿入する方式(データ・数式・入力規則・条件付き書式・列の非表示状態は保持されたまま右へシフトされる、Google Sheetsのネイティブな列挿入動作)。実行後、ヘッダー「製造番号」・列幅・縞模様の背景色を追加設定する。冪等(既に製造番号列がある月はスキップ)。**「不良集計」等の集計シートの数式は、参照先の列がずれたことをGoogle Sheetsが自動追従するため、この関数の実行だけで反映され、作り直し不要。**
- **【未対応・注意】`MigrateOldData.gs`の6-7月移行関数群(`migrateJuneJulyData`等)は旧列構成(D列=得意先名)のまま**。過去の移行は既に完了しており再実行の予定は無いため今回は手を入れていないが、**もし将来6-7月分を再移行する必要が生じた場合は、先にこのファイルの列番号を新構成に合わせて修正すること**(そのまま実行すると1列ずれて書き込まれる)。
- **ユーザー側の残作業**: ①`SetupSpreadsheet.gs`の最新版をGASエディタに貼り替え→`addManufacturingNumberColumn`を1回だけ手動実行(既存12シートに製造番号列を追加)。②`WebApi.gs`の最新版に貼り替えて「新しいバージョン」で再デプロイ。
- **ユーザーからの質問「新年度自動作成にテンプレートは不要か」への回答**: 新年度のスプレッドシートはテンプレートファイルをコピーするのではなく、**現在の2026年度ファイルと同じビルド用コード(`setupQualityDefectSystemFor_`)で毎回組み立て直す**方式。そのため(a)構造は常に現在のコードと完全に一致する(手動でテンプレートを更新し忘れる心配が無い)、(b)入力データは無い状態(空)で自動作成される(ちょうど新年度分として正しい状態)。旧システムがテンプレートファイルをコピーしていたのとは異なる方式だが、結果的に「今の2026年度ファイルと同じ構成のものが、データ無しの状態で自動生成される」という点では要望通りの動作になっている。

### ⑫ ダッシュボードKPIタイルの対象月バグ修正(2026-08-13)
ユーザー指摘: 「一番上の4つのタイルが『今月』表示なのに、実際には今月度(今日の日付)のデータになっていない」。原因を確認したところ、`dashboard.html`の`buildKpiRow`が`monthlyQty[monthlyQty.length-1]`のように**配列の末尾を常に「今月」として表示していた**バグと判明。`monthlyQty`等は年度内の月順(6月始まり)で固定インデックスの配列のため、実際の日付とは無関係に**常に年度最後の月(5月)のデータが表示され続けていた**(西軽精機リポジトリnishikaru-fuguai-dashboard、コミット`03db7df`で修正)。
- **対応方針**: 単に「今月」を正しく指すよう直すのではなく、ユーザー要望通り「前月(直近の完了月)」を表示する仕様に変更(当月はまだ集計途中で数字が小さく見えるため、直近の完了月の方が実態を表す)。
- `reportMonthIndex()`(新設)で今日の日付から対象月のインデックスを算出し、その1つ前(前月)を表示対象にする。年度最初の月(6月)を見ている場合だけ「前月」が前年度分(データ無し)になるため、その月自体を表示するフォールバックあり。
- **タイルのラベルにも対象月名を明示**(「今月 不良個数」→「7月 不良個数」のように)。今後同じ勘違いが起きないよう、どの月のデータかが一目で分かるようにした。「社内不良(KP)比率」タイルは年計値のため「社内不良(KP)比率(年計)」に文言変更。

### ⑬ GASプロジェクトをclasp+GitHubで管理(2026-08-15新設)
これまでGASコードの変更は「ローカルの.gsファイルを編集→内容を丸ごとコピーしてGASエディタに貼り替え」という手作業だったが、ユーザー指示で[[reference_clasp-gas-github-workflow]]の手法([[project_kintai-nishikaru-handover]]で先行実績あり)を導入し、GitHub経由での管理に切り替えた。
- **スクリプトID**: `1hP7RnPp4bH_WbyuXoTU0tarqbtFejwWl21fUlqMUkhjoY9gaNwi9flHd`(このフォルダ直下の`.clasp.json`に設定済み)
- **新規GitHubリポジトリ**: [`n-moriizumi-blip/nishikaru-fuguai-gas`](https://github.com/n-moriizumi-blip/nishikaru-fuguai-gas)(public)。このフォルダ自体を`git init`してpush済み(コミット`c38fd2a`)。`index.html`・`dashboard.html`は別リポジトリ(`nishikaru-kensa-app`・`nishikaru-fuguai-dashboard`)で管理しているため`.gitignore`で除外し、こちらには含めない。
- **`.clasp.json`**: `scriptExtensions: [".js", ".gs"]`により既存の`.gs`ファイルをそのままclasp管理対象にできる([[project_kintai-nishikaru-handover]]の`勤怠管理システム`フォルダと同じ設定)。`.claspignore`は`**/**`→`!*.gs`・`!appsscript.json`のホワイトリスト方式で、`.html`・`CLAUDE.md`等はGASプロジェクトへ誤ってpushされないようにしている。
- **導入時の検証**: `clasp clone`で本番コードを取得し、ローカルの`.gs`ファイルと`diff`した結果、`SetupSpreadsheet.gs`・`WebApi.gs`は完全一致(=これまでの手動貼り替え運用は正しく反映されていた)。`MigrateOldData.gs`だけコメント1行分のズレがあり(2026-08-13にローカル側だけ更新してGASへの反映を忘れていた)、`clasp push`で解消した。
- **【重要な注意点】`clasp pull`は既存の`.gs`ファイルと同名でも`.js`拡張子で新規ファイルを作ってしまう**(実際に発生・削除して対処済み)。今後`clasp pull`を使う場合は、pull後に同名`.js`が重複生成されていないか必ず確認し、あれば内容を比較の上で`.js`側を削除すること。`clasp push`(ローカル→GAS)はこの問題が起きない。
- **【重要な注意点】`clasp push`はGASプロジェクトの中身(コード)を更新するだけで、WebApi.gsが提供するWebアプリの「デプロイ」バージョンは更新されない**。`SetupSpreadsheet.gs`・`MigrateOldData.gs`(GASエディタで手動実行する関数群)への変更は`clasp push`だけで即座に有効になるが、`WebApi.gs`(`doGet`/`doPost`)への変更を実際のWebアプリURLに反映するには、これまで通りGASエディタの「デプロイを管理」→既存デプロイを「新しいバージョン」で再デプロイする操作が別途必要(`clasp deploy`コマンドもあるが今回は未使用・未検証)。
- **今後の運用**: コード変更時は「ローカルの`.gs`を編集→(必要なら)`git commit`&`git push`でGitHubへ反映→`clasp push`でGASへ反映→WebApi.gs変更時のみ追加で手動デプロイ」という流れになる。GASエディタへの手貼り替えは基本的に不要になった。

### ⑭ CC/KP/SK台帳をユーザーが旧システム形式で手作り、年度自動作成の構成を追随(2026-08-15)
`listAllSheets`(診断用、新設)でスプレッドシートの実際のシート一覧を確認したところ、⑤⑥⑦で「方針未確定のため非表示」としていたCC/KP/SK台帳3シートについて、**ユーザーが独自に(コードを使わず手作業で)旧システム形式の複雑な複数行結合ヘッダー構成へ作り直し、表示状態にしていた**ことが判明。加えて、コードが知らない新規シートが2枚追加されていた。
- **実際のシート構成(2026-08-15時点、24枚)**:
  - コード生成の簡易版CC/KP/SK台帳3枚 → ユーザーが名前末尾に「仮」を付けて非表示のまま残していた(例:「客先クレーム管理台帳(CC)仮」)
  - **本物のCC/KP/SK台帳3枚**(「客先クレーム管理台帳(CC)」等、仮なし・表示中)→ 旧システムの「客先クレーム管理台帳(CC)」等と同じ、複数行の結合ヘッダー(タイトル行／品証から・社長から全員へ等の役割行／列見出し行)を持つ形式で手作業により再現。CCは固定行数6・66列、KPは固定行数4・65列、SKは固定行数4・62列。
  - **「データ」シート(新規・非表示)** → 年月日・不良項目名・キズ原因等のプルダウン選択肢の元データ。旧システムの「プルダウンメニュー」シートに相当。固定行数0、45行使用。「異物混入」など現行の40項目マスタ(`DEFECT_ITEMS`)に無い項目も含まれていた(現状は追加せず据え置き)。
  - **「サプライヤー不具合」シート(新規・表示)** → 客先クレーム(CC)・社内不良(KP)のどちらでもない、**仕入先(サプライヤー)起因の不具合**を管理する全く新しいシート。列: 受領日／客先／図番/品名／サプライヤー／全加工or処理のみ／数量／不具合数／不具合内容／単価／不良金額／備考・処置／材質。固定行数0(実際のヘッダーはタイトル行+列見出し行の2行)。
- **年度自動ロールオーバーへの対応方針(ユーザー合意済み)**:
  - コード生成の簡易版3枚(「仮」付き)は**今後も引き続き作成する**(ユーザー希望。本物と名前が衝突しないよう、`buildCcLedgerSheet_`等のビルド関数側のシート名にも「仮」を付けるよう変更した)。
  - 本物のCC/KP/SK台帳3枚・データ・サプライヤー不具合の計5枚は、**結合セル等が複雑でコードでの再現が非現実的なため、コード生成をやめて「前年度のシートをそのままコピーする」方式に変更**。新設の`copyCurrentStructureSheets_(sourceSs, targetSs)`(`SetupSpreadsheet.gs`)が`sheet.copyTo()`で複製し、その後`COPY_STRUCTURE_SHEETS_`設定に従って「見出し行より下のデータをクリアするか(`clearData`)」を判定する。
  - **CC/KP/SK台帳3枚とサプライヤー不具合はデータを空にして構造・プルダウンだけ引き継ぐ**(不良〇月シートと同じ考え方、ユーザー合意)。見出し行数の判定は`sheet.getFrozenRows()`(CC=6・KP=4・SK=4は正しく設定されている)を使うが、**サプライヤー不具合は固定行数が未設定(0)だったため、実際の構成(タイトル行+列見出し行)から2行に決め打ちしている**(`headerRowsOverride: 2`)。ユーザーが手動で構成を変えた場合はこの値の見直しが必要。
  - **「データ」シートは年度を問わない参照用マスタとみなし、`clearData: false`で内容ごとそのまま引き継ぐ**(不良項目マスタ等と同じ「年度に依存しない共通データ」という扱い)。
  - `getOrCreateYearSpreadsheet_`は新年度スプレッドシート作成後、`getOrCreateYearSpreadsheet_(fiscalYear - 1)`で前年度のスプレッドシートを取得し、そこから上記5枚をコピーする(前年度が無い場合はtry/catchで諦め、核となる不良〇月等のシートは作成済みのまま処理を続ける)。
- **【未検証】この年度ロールオーバー処理自体は、次に新年度に入る2027年6月まで実際には走らない**。個々の要素(`getFrozenRows`の値・`copyTo`の動作・フォルダ検索)は確認済みだが、一連の流れを通しでテストしたわけではないため、次回の年度切り替わり時(または任意のタイミングで手動テストする場合)は実際の動作を確認すること。
- **診断用関数`listAllSheets`(新設、`SetupSpreadsheet.gs`)**: 現在の年度のスプレッドシートの全シート名・表示/非表示・行数・列数・固定行数・データ最終行をログ出力する。今回のようにユーザーが手作業でシート構成を変えた場合の状況確認に使える(何度でも安全に実行可能)。
- **`prepareNextYearSpreadsheet`(新設)**: 年度が変わった直後に最初のアクセス(ダッシュボード表示や不具合入力)が新年度スプレッドシートの自動作成(数分かかる重い処理)を引き起こしてしまう問題への対策。次年度分を事前に準備しておく処理本体。
- **【自動化・トリガー設置済み想定】`installPrepareNextYearTrigger`(1回だけ手動実行)**: 「年度が変わる直前(5月)に`prepareNextYearSpreadsheet`を自動実行する」ためのトリガーを設置する。GASの時間主導型トリガーには「年1回」という設定が存在しないため、**毎月25日に実行される月次トリガー**(`autoPrepareNextYearSpreadsheet`)を設置し、その関数内で「今が5月かどうか」を判定して実質年1回に絞る方式にした。これにより`prepareNextYearSpreadsheet`を毎年手動実行する必要が無くなる。`listTriggers`(トリガー一覧確認)・`removePrepareNextYearTrigger`(削除)も用意。**このプロジェクトで初めてトリガーを作る場合、`installPrepareNextYearTrigger`実行時に権限の再承認画面が出ることがある。**

### ⑮ クレーム集計シート新設・ダッシュボードへグラフ追加(2026-08-17)
ユーザーから「旧システムの『グラフ』シートB78:U91・B65:AD71のようなクレーム集計をシートとダッシュボードに作りたい」と依頼。実装前に、GASの調査用関数(`MigrateOldData.gs`に新設、いずれも書き込みなし)で実物を確認してから設計・合意した。
- **`debugInspectClaimSummaryRanges`**: 旧スプレッドシート(`MIGRATE_KP_SOURCE_SS_ID`)の「グラフ」シートB78:U91・B65:AD71を直接ログ出力。結果、B78:U91は「客先クレーム件数」表(月×不良項目カテゴリ18種の件数)、B65:AD71は「客先クレーム内訳」(月ごとの自由記述コメント、集計表ではない)と判明。
- **`debugInspectCcLedgerColumns`**: 新システム側の本物「客先クレーム管理台帳(CC)」シートの実列位置を確認。ヘッダーは3行目(結合含む複数行)、K列(11列目)=クレーム内容分類、C列(3列目、C:E結合)=発生日(受領日)、データは6行目から。K列は入力規則なしの自由入力だが、実際の入力値(寸法不良・キズ・寸法小・切粉等)は現行の新40項目マスタ(`DEFECT_ITEMS`)の項目名とそのまま一致していた。
- **設計方針(ユーザー合意、3点とも推奨案を選択)**:
  1. 集計は新40項目マスタ(`DEFECT_ITEMS`)ベース(旧18分類への回帰はしない)。既存の「不良集計」等と作り方を揃える。
  2. K列(クレーム内容分類)に他の台帳と同じプルダウン(データの入力規則)を追加し、今後の表記ゆれによる集計漏れを防ぐ。
  3. ダッシュボードのグラフは、月別合計件数の推移のみのシンプルな折れ線(項目別内訳までは追加しない)。
- **実装**:
  - `SetupSpreadsheet.gs`に`CC_LEDGER_SHEET_NAME`/`CC_DATE_COL`(C列)/`CC_CATEGORY_COL`(K列)/`CC_DATA_START_ROW`(6行目)/`CC_DATA_END_ROW`(1000行、伸びしろ)を新設。
  - `buildClaimSummarySheet_(ss)`: 「クレーム集計」シートを新設。DEFECT_ITEMS(40項目)×月(6〜5月)で、CC台帳への`SUMPRODUCT((日付範囲<>"")*(MONTH(日付範囲)=対象月)*(分類範囲=項目名))`により件数を集計(CC台帳は月別分割シートではなく通し番号の1本シートのため、不良集計と違いCOUNTIFではなくSUMPRODUCT方式)。個数・金額は無く件数のみ(旧グラフと同じ)。`setupQualityDefectSystemFor_`に組み込み済みのため、来年度以降の新規スプレッドシートには自動的に含まれる。
  - `addClaimSummarySheet()`(1回だけ手動実行): 今年度(2026年度)の現行スプレッドシートにだけ「クレーム集計」シートを追加する。
  - `addCcClaimCategoryValidation()`(1回だけ手動実行): 本物の「客先クレーム管理台帳(CC)」K列にDEFECT_ITEMSベースのプルダウンを追加(`setAllowInvalid(true)`のため既存データやマスタ外の値を壊さない)。
  - `WebApi.gs`の`buildDashboardData_`に、「クレーム集計」シートの合計行(月別12列)を読む`claimMonthly`を追加してJSONに含めた。
  - `dashboard.html`に「客先クレーム件数の月別推移」カード(`cardClaimMonthly`、`buildLine`使用、`--series-2`色)を追加(月別不良金額カードの直後)。
- **【未実行・要ユーザー作業】** `clasp push`でGASプロジェクトへのコード反映は完了済みだが、以下はユーザー側の作業が必要:
  1. GASエディタで`addClaimSummarySheet`を1回実行(「クレーム集計」シートが追加される)
  2. GASエディタで`addCcClaimCategoryValidation`を1回実行(K列にプルダウンが追加される)
  3. `WebApi.gs`のdoGet(mode=dashboard)にclaimMonthlyフィールドを追加したため、**Web Appの「デプロイを管理」→「新しいバージョン」で再デプロイしないとダッシュボード側に反映されない**(clasp pushだけではデプロイ済みバージョンは上がらない)
  4. 上記3ステップ後、品質不良ダッシュボードを開いて「客先クレーム件数の月別推移」カードが正しく表示されるか確認(実機確認、dashboard.htmlは`nishikaru-fuguai-dashboard`リポジトリへpush・反映済み)

### ⑯ ダッシュボードのKP/CC区別・客先クレーム加工者別/検査員別グラフ(2026-08-17)
⑮の「クレーム集計」実機確認完了後、ユーザーから3点追加依頼: (1)ダッシュボードで社内不良(KP)と客先クレーム(CC)のどちらのグラフか分かるようにしてほしい、(2)客先クレームの加工者別グラフ、(3)客先クレームの検査員別グラフ。AskUserQuestionで2点合意(推奨案を選択):
1. **KP/CC区別**: 各カードのタイトルは変えず、`dashboard.html`にセクション見出し(`.section-title`、山吹色`--brand-accent`の縦バー付き)を追加し、「社内不良(KP)・差し戻し」(既存8カード)と「客先クレーム(CC)」(クレーム集計関連3カード)にグループ分け。
2. **加工者別・検査員別の集計値**: NG数の合計ではなく「クレーム件数」(台帳の行数)。既存の「設備別 不良個数」と同じ数え方の思想。
- **実装**: `WebApi.gs`の`buildDashboardData_`に、「客先クレーム管理台帳(CC)」のN列(`CC_WORKER_COL`=14、加工者)・P列(`CC_INSPECTOR_COL`=16、検査員)を件数集計する処理を追加し、`claimByWorker`/`claimByInspector`としてJSONへ含めた(`SetupSpreadsheet.gs`に両列定数を追加)。`dashboard.html`に`cardClaimByWorker`/`cardClaimByInspector`(`buildRanking`使用、`--series-2`色)を追加。
- **副産物のバグ修正**: `addClaimSummarySheet`の実機実行後、「クレーム集計」シートの全セルが`#VALUE!`エラーになる不具合が判明。原因は`buildClaimSummarySheet_`の集計式`SUMPRODUCT((日付範囲<>"")*(MONTH(日付範囲)=対象月)*...)`で、日付範囲に「日付として解析できないテキスト」が1件でも混じっていると、掛け算で0倍される前に`MONTH()`自体が配列全体に`#VALUE!`を伝播させてしまうこと(SUMPRODUCTは要素ごとのAND判定であってもエラー値の伝播は防げない)。`MONTH(日付範囲)`を`IFERROR(MONTH(日付範囲),0)`に変更して解消(該当セルは「どの月とも一致しない0」として扱われるだけになり、他の行の集計への影響が無くなる)。
- **【未実行・要ユーザー作業】** `addClaimSummarySheet`を再実行(修正済みの数式で「クレーム集計」シートを作り直す)。Web Appの「新しいバージョン」再デプロイ(claimByWorker/claimByInspectorをダッシュボードに反映するため)。反映後、新しい2グラフとセクション見出しの表示を実機確認。

### ⑰ ダッシュボードからKP/差し戻し比率グラフを削除、SK台帳の日付列を修正(2026-08-17)
- **グラフ削除**: ユーザー依頼で「社内不良(KP)と差し戻しの比率」の単独グラフ(`cardKpRework`)を`dashboard.html`から削除、未使用になった`build100StackedBar`関数も削除。**KPIタイル「社内不良(KP)比率(年計)」は同じデータ(`kpReworkRatio`)を引き続き使うため、`WebApi.gs`側の計算・返却はあえて残した**(削除対象はグラフのみで、KPIタイルは対象外とユーザーに確認済み)。
- **CC/KP/SK台帳の日付入力**: ユーザーから「年月日の入力がプルダウンになっている、ダブルクリックでカレンダー入力できるようにしたい」と依頼。`debugInspectDateColumnValidation`(調査用、新設)で3台帳の「日」を含む見出し列の入力規則を実機確認したところ、**CC・KPの発生日列(C列)は既に正しい日付検証(`requireDate()`)が設定済み**で、ユーザーが「プルダウン」と感じていたのはこのカレンダーアイコンのことだったと判明。**SK台帳のC列(発生日(品質会議開催日))だけ入力規則が無く、表示形式も日付ではない生の数値書式(`0.###############`)**という不具合が見つかった。Googleスプレッドシートの仕様上「ダブルクリックで直接カレンダーが開く」動作は作れない(ダブルクリックは文字編集モードに入るだけ、日付検証があるとセル選択時に出るカレンダーアイコンをクリックする形になる)ことをユーザーに説明し、SK列をCC/KPと同じ設定に揃える方針で合意。`fixSkDateColumnValidation`(1回だけ手動実行)を新設: SK台帳C列(5〜154行目)に`requireDate()`+ヘルプテキスト+CC/KPと同じ表示形式(`yyyy"年"m"月"d"日"`)を設定。既存データの値自体は変更しない。
  - 診断関数の初版に不具合があった点も記録: 見出しセルの装飾用全角スペース詰め("発生日　　　　（受領日）"のような長い文字列)を元の文字列長で足切りしていたため、CC台帳の該当列を最初は検出できなかった。空白類を除去してから判定するよう修正して解消。
- **【未実行・要ユーザー作業】** `fixSkDateColumnValidation`を1回実行。Web Appの新しいバージョン再デプロイ(グラフ削除の反映)。

### ⑱ ダッシュボードの「設備別 不良個数」を「加工者別 不良件数」に変更(2026-08-17)
ユーザー依頼で、不良〇月シートのH列(機種名)ベースの個数集計(`machineQty`)から、G列(加工者)ベースの件数集計(`workerDefectCount`)に変更。追加不良項目の行はG列(加工者)が空欄のままなので二重カウントされない(処置区分(B列)のように複製していないため)。`WebApi.gs`の`machines`フィールドは`workers`に改名、`dashboard.html`の`cardMachineRanking`→`cardWorkerRanking`、`MACHINES`→`WORKERS`も合わせて変更。

### ⑲ CC/KP/SK台帳の全面リニューアル(2026-08-18)
ユーザーから「客先クレーム管理台帳(CC)と社内不良管理台帳(品証)(KP)と社内不良管理台帳(製造工程)(SK)で年月日の入力がプルダウンになっているのを直せるか」と依頼(⑯参照)。実機のxlsxダウンロード(`unzip`でzip展開し、OOXMLのセル・マージ・入力規則をnodeスクリプトで解析)で構成を精査したところ、当初想定していた「発生日1箇所だけの問題」ではなく、**不適合改善計画書ワークフロー欄(なぜなぜ分析〜水平展開)の完了日9箇所が、いずれも「⇒矢印+月プルダウン+日プルダウン」の3列構成(年の記録なし、データ!E/F列参照)**だったと判明。ユーザーは「この3台帳は構成がよくないので新規に作り直す方がよいのでは」と方針転換し、以下で合意:
- 現行の台帳(本物)は稼働継続、新デザインを別シートとして作り、確認後に切り替える
- 見た目は文字色・背景色・線をフル活用して見やすく仕上げる、実装前にモックアップで確認する
- 担当者は全ステップでプルダウン化する

**モックアップ(Artifact)での往復**: https://claude.ai/code/artifact/7a5e8b8a-30d6-429e-9672-8f28f7fc022e で3往復のフィードバックを反映:
1. 「関連文書は客先からの連絡票等とその他文書・ロット番号がある」「品番の後に品名」「NG数の後に納入数」「機械名の後に機械番号」→ xlsxのマージセル(`<mergeCells>`)を精査した結果、現行は**1レコードが2物理行にまたがり、品番/品名・NG数/納入数・機械名/機械番号が同じ列に2段で詰め込まれている**ことが判明。新デザインでは独立した列に分離。
2. 「確認部署が3ヶ所ある」「モックアップの見出しがずれている」→ CCのxlsxで「改善点の効果確認」列見出しがAF/AH/AJの3箇所に繰り返されていた理由を確認したところ、**加工者・品証・社長の3部署がそれぞれ確認する構成**と判明(旧デザインは同じラベルが3回並ぶだけで分かりにくかった)。あわせてモックアップの`colspan`ずれ(列追加時の直し忘れ)も修正。

**最終フィールド構成(実機調査で確定)**:
- CC: 台帳番号・発生日(受領日)・関連文書(客先からの連絡票等／その他文書・ロット番号)・客先名・品番・品名・クレーム内容分類・クレーム内容詳細・NG数・納入数・加工者・機械名・機械番号・検査員・出荷区分・単価・金額
- KP: 台帳番号・発生日(受領日)・関連文書(その他文書(客先等))・客先名・品番・品名・クレーム内容分類・クレーム内容詳細・不良数・加工数・加工者・機械名・機械番号・検査員・単価・合計(出荷区分は無し)
- SK: 台帳番号・発生日(品質会議開催日)・関連文書(製造工程不良一覧／その他文書(客先等))・客先名・品番・品名・不良層別区分・不良詳細・不良数・加工者・機械名・機械番号(検査員・出荷区分・単価・金額は無し)
- 共通ワークフロー(9ステップ、各「担当」+「完了日」): ①なぜなぜ分析②改善策の共有③図面に貼付④改善の実施⑤効果確認(加工者)⑥効果確認(品証)⑦効果確認(社長)⑧水平展開の実施⑨水平展開の効果確認、+水平展開の判定(〇/×)・差し戻し文書No.・備考

**実装(`SetupSpreadsheet.gs`)**:
- `LEDGER_WORKFLOW_STEPS`(9ステップ名)・`HANKO_CHOICES_`(〇/×)・`SHUKKA_KUBUN_CHOICES_`(出荷区分)を新設。旧`WORKFLOW_HEADERS`は廃止。
- `fetchDataSheetLists_(ss)`: このスプレッドシート自身の「データ」シートC列(全社員名簿、ワークフロー「担当」用)・I列(検査員名簿)を読み取る(見出しらしき文字列の混入は除外)。機械名・加工者名は従来通り組織図マスタ(`fetchOrgMasterLists_`)を正とする。
- `buildLedgerSheetV2_(ss, sheetName, recordFields, staffList, formulaCols)`: 旧`buildImprovementLedgerSheet_`を全面作り直し。2行ヘッダー(グループ行+列見出し行)・ステップごとの色分け(`GROUP_PALETTE`+1色)・完了日/発生日は`requireDate()`による単一セルの日付検証・全担当列を共通プルダウン化。
- `buildCcLedgerSheetV2_`/`buildKpLedgerSheetV2_`/`buildSkLedgerSheetV2_`: 上記の実機調査で確定したフィールド構成でシートを組み立てる。
- `addRedesignedLedgers()`(1回だけ手動実行): 3台帳のリニューアル版を「（新）」付きシート名で追加(既存の本物には触れない)。
- `swapToRedesignedLedgers()`(要ユーザー確認後に1回だけ手動実行): 「（新）」シートを正式名称に切り替え、現行シートは「（旧）」を付けて非表示のまま残す(削除はしない)。
- `removeOldTemporaryLedgerSheets()`(1回だけ手動実行): 旧デザインのコード生成「仮」台帳3枚を削除(ユーザー確認済み、`setupQualityDefectSystemFor_`は今後もリニューアル版の仮シートを自動生成するため支障なし)。
- **列参照の動的解決**: 台帳のデザインが変わると列位置がずれるため、`resolveCcLedgerColumns_(sheet)`(見出し文字列から発生日・クレーム内容分類・加工者・検査員の列番号を動的に求める、装飾用空白は除去してから部分一致)を新設し、`buildClaimSummarySheet_`・`addCcClaimCategoryValidation`・`WebApi.gs`の加工者別/検査員別集計はすべてこれ経由に変更(固定列番号の`CC_DATE_COL`等は廃止)。旧デザイン・新デザインのどちらが稼働中でも自動的に追随する。
- **実装後の実機トラブル対応(2026-08-18)**:
  1. `addRedesignedLedgers`初回実行時、`buildLedgerSheetV2_`の`setFrozenColumns(3)`が「結合されたセルの一部だけを含む列を固定することはできません」で失敗(記録情報欄の見出しを全列結合していたため)。列固定を記録情報欄の全列(`recordColCount`)に広げて回避したが、今度は固定エリアが横に広すぎてGoogleシート側が「ウィンドウが小さすぎて表示できない」警告を出す実機不具合が発生。
  2. ユーザー判断で「列固定は不要、一番左のままでよい」「記録情報欄の見出しはA〜R等を結合したい」「列幅はデータ(見出し文字列)に合わせて自動調整」の3点に方針転換。`setFrozenColumns`を撤去(列固定なし)、記録情報欄の見出し結合を復活(列固定の制約が無くなったため両立可能に)、固定ピクセルの列幅指定を`sheet.autoResizeColumns(1, lastCol)`に置き換えて解決。
- **【完了、2026-08-18】** `addRedesignedLedgers`→内容確認→`swapToRedesignedLedgers`→`removeOldTemporaryLedgerSheets`→Web App新バージョン再デプロイ、を一通り実行済み(ユーザー確認済み)。CC/KP/SK台帳は正式にリニューアル版へ切り替わった。旧台帳の入力済みデータ(進行中のCAPA案件)はユーザーが手作業で新シートへ移行済み。
- **移行後に発覚した`resolveCcLedgerColumns_`のバグ(2026-08-18)**: 「加工者別件数が反映されない」との報告を受け`debugInspectCcLedgerData`(新設、書き込みなし)で調査したところ、`resolveCcLedgerColumns_`の見出し検索が**部分一致のみ**だったため、記録情報欄の本来の「加工者」列(L列)より先に、ワークフロー欄⑤「効果確認**(加工者)**」という見出し(AA列、なぜなぜ分析等と同じ行1のグループ見出し)に誤って部分一致してしまっていた(検査員・クレーム内容分類・発生日は同名の見出しが他に無かったため問題なし)。**完全一致を優先し、見つからなければ部分一致にフォールバックする**方式に修正して解消。同種のバグは今後、記録情報欄の見出し文字列がワークフロー欄のステップ名(なぜなぜ分析・改善策の共有・図面に貼付・改善の実施・効果確認・水平展開の実施・水平展開の効果確認)の一部と重なる場合に再発しうるので注意。
- **本番運用開始に伴う付随対応**: 品質保証課アプリ(`nishikaru-kensa-app`)の`app/defect-report/index.html`(不具合入力画面)の送信完了画面に、初期プロトタイプ時代の「この画面はデモ版です」「入力内容を受け付けました(デモ)」「送信APIはまだ接続されていません」という文言が実装完了後もずっと残っていたことが判明(実際のAPIは正常動作していた、UI文言のみが古いままだった)。実態に合わせて削除・修正しGitHub Pagesへpush済み。

### ⑳ キズ原因欄の条件表示・必須化(2026-08-18)
ユーザーから2点質問: (1)不良内容の入力に編集機能はあるか→**無い**(送信のたびに新しい行が追加されるのみ、既存記録の修正はスプレッドシート側を直接編集する運用)と回答。(2)キズ原因欄を、不良項目でキズ系が選ばれたときだけ表示・必須にできるか→AskUserQuestionで2点合意(推奨案採用): ①処置区分=社内不良(KP)の条件と**AND**で組み合わせる(差し戻しでは従来通り使わない)②不良項目プルダウンで**選択中の値**と**追加済みチップのいずれか**の両方を判定対象にする(複数項目を追加できる仕様のため)。
- **実装(`nishikaru-kensa-app`の`app/defect-report/index.html`)**: `KIZU_KEI_DEFECT_ITEMS`(キズ・流動キズ・チャックキズ・回収時キズ・落下キズ・材料キズ・加工時キズ・ガイドブッシュキズ・その他キズの9項目)を新設。`updateKizuGenInVisibility()`が処置区分・選択中項目・追加済み`items`配列を見て表示/非表示・ラベル文言(「キズ原因」⇔「キズ原因(必須)」)・入力値クリアを切り替える。呼び出し元: `selectShochi()`(処置区分変更時)・`item-select`のchangeイベント(項目選択時)・`addItem()`/`removeItem()`(追加・削除時)。`submitForm()`に、表示中なのに未選択なら送信をブロックするバリデーションを追加。
- **【同日中に方針修正】** ユーザーから「処置区分に関係なくキズ系が選ばれたら表示」「(必須)表記は不要(フォームの他項目も基本必須のため)」と指示があり、`updateKizuGenInVisibility()`から処置区分の条件を削除(不良項目がキズ系かどうかだけで判定)、ラベルの動的な「(必須)」切り替えも削除して常に「キズ原因」表記に統一。
- **【未確認・要ユーザー作業】** GitHub Pagesへの反映を待って(数分)実機で動作確認すること(まだブラウザでの実地確認は行っていない)。

### ㉑ 送信済み記録の編集機能(2026-08-18新設)
ユーザーから「編集ができるようにしたい」と依頼。AskUserQuestionで3点合意(いずれも推奨案または明示的な指示):
1. 検索方法: QRスキャン(または手入力)した製造番号について、ログイン中の本人が過去に送信した記録があれば、**フォームに自動で読み込んで編集モードにする**(一覧から選ぶ方式ではない)
2. 編集権限: **自分の送信分だけ**編集可能(品証担当者列が本人と一致する記録のみ)
3. 誤って上書きする事故を防ぐため、**「新規入力にする」ボタン**を用意し、別の新規不良を入力したい場合はワンタップで編集モードを解除できるようにする

**実装**:
- `WebApi.gs`: 不良〇月シートのW列(送信ID、これまで「今回未使用」だった列)に、全送信で`Utilities.getUuid()`を書き込むように変更。これをキーに記録を後から特定できるようにした。
  - `writeDefectRecord_`と共通の行書き込み処理を`writeRecordRows_`に切り出し(新規送信・更新の両方から呼ぶ、列構成が二重管理でズレるのを防ぐため)。
  - `action=lookupRecord`(POST): 製造番号+本人一致で、**今月・先月の「不良〇月」シートのみ検索**(全12シート走査は重いため。直近の入力ミス修正を想定、古い月は編集対象外)。複数マッチがあればタイムスタンプが最新のものを返す。送信IDが無い(=この機能導入前の)記録は編集対象外。
  - `action=update`(POST): `lookupRecord`で見つけた送信ID・月を指定して上書き。**品証担当者列をサーバー側でも本人と照合**(クライアント側の制御だけに頼らない)。新しい不良項目数が元の行数より多い場合、直後の行が本当に空いているか確認してから使う(隣接する別の記録を巻き込まないための安全策)。**K・R・U列(良品数・金額・不良率の数式)を誤って消さないよう、クリア処理は列を分けて行う**(実装中に自分で気づいて修正した箇所、要注意ポイント)。送信ID・タイムスタンプは更新時も維持する。
- `index.html`(`nishikaru-kensa-app`): QRスキャン/手入力の直後に`tryLoadExistingRecord`が`lookupRecord`を呼び、見つかれば`populateFormFromRecord`でフォーム全体(処置区分・機種名・設備№・加工者・加工数・材質・単価・不良項目一覧・キズ原因・備考)を復元し、黄色い編集バナー(「以前の入力を編集しています」+「新規入力にする」ボタン)を表示。送信時は`editingId`の有無で`action=submit`/`update`を切り替える。完了画面の見出しも編集時は「内容を更新しました」に変わる。
- **【未確認・要ユーザー作業】** `WebApi.gs`の変更(doGet/doPost)を反映するため、**Web Appの「デプロイを管理」→「新しいバージョン」で再デプロイが必要**。GitHub Pages側(`index.html`)は数分で自動反映。反映後、実機で一連の流れ(QRスキャン→前回内容の自動読み込み確認→編集して送信→シートで上書きされたか確認、および「新規入力にする」ボタンでの新規入力も)を確認すること(まだ実機確認していない)。

### ㉒ 不良〇月のキズ原因列をV列からN列へ移動(2026-08-18)
ユーザー依頼: 「不良〇月でキズ原因(任意)の(任意)は不要で、VからN列に移動して。非表示列はIDだけで良い」。列位置の変更は影響範囲が広い(K・S・V列の数式、不良集計・不良集計(キズ原因)・月次サマリーの参照式、WebApi.gsの書き込み/読み込み列)ため、実装前に新しい列構成(A〜Wの全23列)を組み立てて把握してから着手。

**新しい列構成**: A タイムスタンプ／B 処置区分／C 品証担当者／D 製造番号／E 得意先名／F 品番(図番)／G 加工者／H 機種名／I 設備№／J 加工数／K 良品数(数式)／L 不良数計／M 不良項目／**N キズ原因**(移動後、常時表示)／O 不良数／P 不良項目詳細／Q 担当者2／R 単価／S 金額(数式)／T 備考／U 材質／V 不良率(数式)／W 送信ID(非表示はこれだけに)。

**実装**:
- `SetupSpreadsheet.gs`の`buildDefectMonthlySheet_`: 見出し配列・列幅・`hideColumns`(23列目のみ)・良品数/金額/不良率の数式生成先(K/S/V)を新構成に更新。
- `moveKizuGeninColumnToN()`(1回だけ手動実行、新設): 既存2026年度の12シートに対し`sheet.moveColumns(V列全体, 14)`でキズ原因を移動。**`moveColumns`はドラッグでの列移動と同じ扱いのため、K・S・V列の数式が参照するセルもGoogle Sheets側が自動的に追従し、コード側で個別に直す必要が無い**。冪等(既に移動済みならスキップ、V列が想定外の内容なら警告して何もしない安全策あり)。
- `buildDefectSummarySheet_`のSUMIF参照先をN→O(不良数)、`buildKpCauseSummarySheet_`のCOUNTIF/SUMIF参照先をV→N(キズ原因)、`buildMonthlySummarySheet_`の金額SUMIFS参照先をR→Sに、それぞれ更新。
- `WebApi.gs`: `writeRecordRows_`(新規送信・更新共通の書き込み処理)・`readRecordFromRows_`(編集機能の記録復元)・`updateDefectRecord_`のクリア列範囲・`buildDashboardData_`の得意先別金額集計、すべて新しい列位置に合わせて更新。入力アプリ側(`index.html`)はフィールド名ベースのJSON契約のため変更不要。
- **【未実行・要ユーザー作業】** ①GASエディタで`moveKizuGeninColumnToN`を1回実行(既存データの列移動) ②Web Appの新しいバージョン再デプロイ(WebApi.gsの列位置変更を反映) ③実機で新規送信・編集の両方を確認し、キズ原因欄がN列(不良項目の右隣)に表示され、金額・不良率が引き続き正しく計算されているか確認すること(まだ実機確認していない)。

## 5. 未確定事項
- **【2026-08-12・大きめの検討事項】改善計画書(なぜなぜ分析〜水平展開)のアプリ化をユーザーが提起**。現状把握のため「改善計画書台帳」フォルダ(ID `1zWL_FZ_yMjx5pTDyK6fdQf0Tdkf-wvrC`)を調査したところ、想定より本格的なQMS帳票と判明: テンプレートは「不適合改善計画書テンプレート NPL60FG-20」(品質文書として改訂履歴管理、ID `19Mt-iw4yrkIfqS3yDmHScsQcSrBXIJaN3HbvwqTayTw`)、発生状況→現物処置→調査結果(なぜなぜ分析・特性要因図)→恒久策(発生対策/流出対策)→水平展開→仕組みへの反映→是正完了後の確認→有効性確認(3ヶ月後 or 3ロット後)という多段階承認フロー(作成者→上原さん等の承認→効果確認→水平展開)を持つ。現状は「(CC)客先クレーム改善計画書」「(KP)社内不良改善計画書」フォルダ配下に案件ごとのサブフォルダ(テンプレコピー＋証拠写真)を作る運用で、全CC/KP案件のうち該当するものだけ作成(多くは「改善計画書対象外」)。ユーザーとは「まず合意済みのグラフ実装(⑧)を完了させ、アプリ化の方針は別途後日あらためて検討する」ことで合意済み(2026-08-12)。着手時は改めてスコープ(Phase分け・承認フローのデジタル化範囲等)を協議すること。
- 送信(action=submit)の実機での一連の動作テストがまだ済んでいない(ログイン→QRスキャン→フォーム入力(単価・詳細・キズ原因含む)→送信→シートへの反映確認)
- アプリアイコンのデザイン(会社ロゴ流用 or 新規デザイン)
- KPの原因マスタ(A〜H)・担当者2などの見出しもI-Pro表記との統一が必要か(現時点では得意先名・品番・材質のみ統一済み)

## 5a. 不良〇月シートのレイアウト統一(2026-08-12)
ユーザーが「不良7月」の列幅を手動で調整したため、他の11ヶ月のシートもそれに合わせる依頼があった。`SetupSpreadsheet.gs`に手動実行用の`syncMonthlySheetLayout`を追加: 「不良7月」の実際の列幅を読み取って他の11シートへ適用し、あわせて全シート・全セル(データ行含む)を水平・垂直とも中央揃えにする(書式のみの変更、データには一切触れない)。

## 6a. 6月・7月の過去データ移行(2026-08-11着手)
ユーザーから、新システム移行前に運用していた旧スプレッドシートの6月・7月分データを「不良〇月」へ引き継ぎたいとの依頼があり、一括移行用の`MigrateOldData.gs`を新設した。
- **移行元**: 「客先クレーム(CC)・社内不良(KP)管理台帳 2026年度」(ID `1JRnbkwY5VF93dLTpyl2yRvaz5XOom6vM4_C6x54o5cM`)の「社内不具合6月」「社内不具合7月」シート(社内不良(KP)分)、および「品証さん差し戻し品の不良数入力」(ID `1xhBS77e1Jk3tJUSlNBmmIqHGlWXNl7fibQgqKaTPtFQ`)の「差し戻し品一覧表」(差し戻し分、年度2026・月6or7の行のみ)。
- **実行方法**: `SetupSpreadsheet.gs`と同じGASプロジェクトへ貼り付け、まず`previewJuneJulyMigration`(書き込みなし、ログ確認用)→問題なければ`migrateJuneJulyData`(実書き込み)の順に実行する。
- **既知の割り切り**: 差し戻し側の旧データには「加工数」列が無いため加工数(I列)は空欄のまま。旧項目名が新マスタ(29項目)に一致しない場合は近いカテゴリ(「その他の外観不良」等)に丸め、元の項目名は不良項目詳細列に残す。品証担当者(C列)は旧データの「検査担当者」「担当」の文字列をそのまま転記(ログイン認証済みの氏名ではない)。移行行は備考欄冒頭に`[旧データ移行]`と付記して見分けられるようにしている。
- **実機での調査・修正(2026-08-11)**: `previewJuneJulyMigration`実行→KP側で「見出しが見つからない」エラー。`debugInspectSources`(調査専用、書き込みなし)を追加して実物のヘッダー行を確認したところ、①ヘッダー行は先頭行ではなく4行目(結合セルのタイトル行が上にある)、②列名は「不適合内容1」ではなく「**不具合**内容1」(推測が1文字違っていた)、③1件の不良に3項目以上ある場合は図番が空欄の追加行で表現される(2項目までしか拾えていなかった)、という3点のズレが判明し、いずれも修正済み。また差し戻し側は年度2026・月ごとの件数を確認した結果、**最新データが2026年2月までで、6月・7月分は1件も無い**ことが判明(別の記録手段に移った可能性。ユーザーへ確認要)。副産物として、KP側のS列に旧コード.jsの「キズ原因(scratchRaw)」がそのまま残っている行があることも分かった(例:「B.チャック内の切粉による圧痕」)ため、新システムのキズ原因(U列)への逆輸入も追加対応として可能(現時点では未実装、必要なら追加対応)。
- **差し戻し側は移行対象なしで確定(ユーザー確認済み、2026-08-11)**。6月・7月はKP分のみ移行する。
- **不良項目名の分類精度向上(2026-08-11)**: 旧KPデータの不良項目名は自由記述(例:「φ7(-0.01/-0.04)チャックキズ」)で新マスタ29項目とほとんど一致しなかったため、`mapDefectItemName_`にキーワード判定(圧痕・打痕・溶着・段差・ネジ不良等を症状語から判定、箇所語より症状語を優先)を追加。旧Googleフォームの「キズ原因」タグも補助判定に利用。それでも不明な場合のみ「該当項目以外の不良」に丸める(元テキストは常に不良項目詳細列に保持)。
- **1回目の本実行(`migrateJuneJulyData`)後、「不良集計(キズ原因)」が0件のままとユーザーから指摘(2026-08-11)**。原因は移行スクリプトがU列(キズ原因)を「旧データに対応する情報が無い」という誤った前提で常に空欄にしていたこと。実際は旧KPシートのS列相当(不良率の2列後ろ)に旧Googleフォームの選択値がそのまま残っていることが判明したため、`mapScratchToKizuGenin_`でプレフィックス(「B.」等)を除いてKP_CAUSE_ITEMSと突き合わせ、一致すればU列へ転記するよう修正。あわせて、移行で書き込んだ全行(メイン行・追加行とも)のV列(送信ID)に目印`MIGRATE_MARKER`を書き込むようにし、`undoJuneJulyMigration`関数でその目印の行だけを安全にクリアしてやり直せるようにした。
- **重複データの真因判明・修正(2026-08-12)**: ユーザーから「移行先データが同じ内容2個ずつになっている」と指摘。当初は`clearMonthSheetDataForRedo`を挟まずに`migrateJuneJulyData`を2回実行してしまった事故を疑い、`previewDuplicateCleanup`/`removeDuplicateMigratedRows`(MIGRATE_MARKERの有無で仕分けて重複を消す)を用意したが、実行すると印あり行しかなく(印なし0件)、この仮説は誤りと判明。
  - **真の原因**: 旧「社内不具合〇月」シートは、1件の不良につき「不具合内容1」(G列、カテゴリ的な短い言い方)と「その他不具合内容」(J列、詳細な言い方)の**2つの列で同じ1件を2通りに表現**しており、数量も「不具合数」がH列・K列の両方に同じ値で二重に入る仕様だった(旧コード.jsの`ngQty1`がH・Kどちらにも書き込まれる作りだったため)。`extractKpRecords_`はこれを「別々の2件の不良項目」として扱っていたため、件数・不良数計が実質2倍になっていた(継続行(3件目以降)は本当に別項目なので、この問題は無い)。
  - **修正**: メイン行からは不良項目を1件だけ作るよう変更。より具体的な「その他不具合内容」を優先して分類に使い、両方の原文を不良項目詳細に残す(`(旧内容:G／J)`形式)。
  - 2026-08-12時点: 修正版で移行をやり直す必要がある(`clearMonthSheetDataForRedo`→`migrateJuneJulyData`→`addRecordBordersToAllMonths`の順)。

## 6. 加工ナレッジプロジェクトとの関係(2026-08-02追記)
別フォルダ `加工ナレッジ\CLAUDE.md` に検討状況あり。要点:
- 正式名称(仮)は「西軽精機ナレッジ」。図番を入力すると、①過去トラ(不具合改善計画書フォルダ)②品質情報スプレッドシート③新システム内の承認済み「コツ投稿」をまとめて表示するAndroidアプリ＋GAS Web Appのシステム。
- I-PRO本体には手を入れず、完全に独立したシステムとして構築する方針(ベンダー協力なしにI-PRO改造は困難と判断済み)。
- **過去トラの表示元データは「不具合改善計画書フォルダ」**(フォルダ名「{得意先} {図番} {品名} 不具合改善計画書」)。これは本プロジェクトのPhase 3(不適合改善計画書の管理)と対象データが重なるため、Phase 3に着手する際は加工ナレッジ側の「図番の部分一致検索でDriveをその場検索する」設計と整合を取れないか検討する余地がある(二重実装を避けるため)。
- 2026-08-02時点で加工ナレッジ自体もまだ「設計案の最終承認待ち」の段階で、Android/GASのコードは1行も書かれていない。本プロジェクトのPhase 4着手時は、まず加工ナレッジ側の進捗を再確認すること。
